package $package$

/** Types for modelling this service's GraphQL API.
  *
  * These are _not_ types for the database schema; source code for those are generated.
  * Rather, these are the types that underlie [[schema the GraphQL schema types]].
  */
package object model {}
